import { type Config } from "prettier";

const config: Config = {
	singleQuote: false,
	semi: true,
	trailingComma: "es5",
	printWidth: 100,
	tabWidth: 4,
	useTabs: true,
	endOfLine: "auto",
	bracketSpacing: true,
};

export default config;
